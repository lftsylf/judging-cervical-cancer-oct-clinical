# Paper v4 训练脚本

> **日期**：2026-07-20  
> **分支**：`feature/paper-v4`

## 数据协议（已定）

| CSV | 用途 |
|-----|------|
| `train_*.csv` | 训练 |
| `val_*.csv` | **早停 / 选模**（内部验证） |
| `external_*.csv` | **仅终评**（训练过程不用） |
| `development_*.csv` | 仅作划分源；训练不再读 |

早停规则：连续 10 个 epoch **内部 val 的 ROC-AUC** 不提升则停；`best_model.pth` 对应 val AUC 最高的权重；external 只在训练结束后评估一次。

## B1 T0（先跑）

等权 mean pooling + 患者级 EDL，对齐 v3 baseline 精神，关 WMA/EMA/Aux。

```bash
export PATH="/home/amax/anaconda3/bin:$PATH"
cd /ssd_data/tsy_study_venv/OptiGenesis_Lancet
./run_experiment.sh --detach ./tsy_loho ./scripts/paper_v4_run/run_b1_mean_edl_t0_oct_only.sh
```

产物：`outputs/paper_v4/baseline/b1_mean_edl_t0/{huaxi,liaoning,xiangya}/seed_*/`

## Ours T0（帧级不确定加权，无稳定模块）

```bash
export PATH="/home/amax/anaconda3/bin:$PATH"
cd /ssd_data/tsy_study_venv/OptiGenesis_Lancet
./run_experiment.sh --detach ./tsy_loho ./scripts/paper_v4_run/run_ours_uw_agg_t0_oct_only.sh
```

产物：`outputs/paper_v4/baseline/ours_uw_agg_t0/{huaxi,liaoning,xiangya}/seed_*/`

关键环境变量（脚本已设好；自己单次跑时也可手动 export）：

| 变量 | Ours | B1 |
|------|------|-----|
| `OPTIGENESIS_FRAME_AGG` | `uncertainty_weighted` | `mean` |
| `OPTIGENESIS_FRAME_AGG_TEMP` | `0.5`（τ） | 无影响 |
| `OPTIGENESIS_USE_WMA` / `ENABLE_AUX` / `ENABLE_EMA` | `0` | `0` |
| `OPTIGENESIS_USE_CLINICAL` | `0` | `0` |

看日志：

```bash
tail -f logs/detached_latest.log
tail -f outputs/paper_v4/baseline/ours_uw_agg_t0/huaxi/seed_42/logs/train_console.log
```
