# Paper v3 冻结说明

> **冻结日期**：2026-07-18  
> **阶段**：第三版整稿实验冻结（ResNet50 OCT-only + WMA/EMA/Aux 消融 + SOTA 对比）  
> **状态**：只读归档 — 第四版禁止往本目录写入新训练产物

---

## 1. 对应代码快照

| 项 | 值 |
|----|-----|
| Git tag | `paper-v3-muse-full` |
| 冻结分支（历史） | `feature/v2-resnet-baseline` |
| 数据软链（冻结时） | `tsy_loho` → `/ssd_data/CervixOCT_datasets/tsy_loho` |
| CSV 快照 | `data/snapshots/paper_v3_tsy_loho/`（同日拷贝） |

复现：

```bash
git fetch origin
git checkout paper-v3-muse-full
git rev-parse --short HEAD
```

---

## 2. 本目录收录内容

| 子目录 / 文件 | 说明 |
|---------------|------|
| `baseline v2/` | v2→v3 baseline 迭代（含 OCT-only T0 定稿） |
| `消融实验v2单模态/` | 2³ 消融 + Full（主表） |
| `对比试验v2单模态/` | SOTA 骨干对比（主表） |
| `outputs_baseline_v1多模态/` | v1 历史 baseline |
| `消融实验v1多模态/` | v1 历史消融 |
| `对比试验/` | v1 多模态对比（勿与 v2 单模态混表） |
| `MUSE_论文实验细节知识库.md` | 面试/叙事向知识库副本（冻结日拷贝） |
| `tableX_*.csv/.tex` | 数据特征表草稿 |

---

## 3. 主结果锚点（便于快速回顾）

- v1 Swin 多模态外部 AUC ≈ **0.59**
- v3 baseline（ResNet50 OCT-only）≈ **0.62**（相对 v1 **+0.03**）
- v3 MUSE Full（+WMA+EMA+Aux）≈ **0.65**（相对 baseline **+0.03**）

协议要点：LOHO 三折 × 5 seeds；当时 `val` 使用 `external_*.csv`（第四版将按审稿意见改为内部 val）。

---

## 4. 与第四版的关系

- 第四版新产物请写到：`outputs/paper_v4/`
- 第四版开发分支：`feature/paper-v4`
- 本归档仅作对照与复现，**不要**在 `第三版/` 下续跑训练

---

*文档生成日期：2026-07-18*
