# T1 ResNet50 定稿（T2 组 1：LR=5e-5）

- **日期**：2026-05-18
- **脚本**：`run_baseline_t1.sh`
- **依据**：`outputs_baseline_t2_lr_resnet50/` T2 探路 → **组 1 全网 5e-5** 三折外部 AUC 均值最高（0.5761）

## 协议

| 项目 | 设置 |
|---|---|
| 骨干 | `resnet50` |
| 学习率 | 全网 `OPTIGENESIS_LR=5e-5`（不设 BACKBONE_LR / HEAD_LR / FREEZE） |
| POS_WEIGHT | 1.3（`lancet_config` 默认） |
| 折 × seed | 华西 / 辽宁 / 湘雅 × 42, 123, 2024（9 次） |
| 早停 | patience=10 |
| 开关 | 无 aux / EMA / WMA / CORAL |

## 输出目录

- **本次（新）**：`outputs_baseline_t1_v2_resnet50_g1_lr5e5/`
- **上次 ResNet50 T1（对比用）**：`outputs_baseline_t1_v2_resnet50/`

## 启动（SSH 断开不影响）

```bash
cd /ssd_data/tsy_study_venv/OptiGenesis_Lancet
export PATH="/home/amax/anaconda3/bin:$PATH"

./run_experiment.sh --detach \
  /ssd_data/tsy_study_venv/OptiGenesis_Lancet/tsy_loho \
  ./run_baseline_t1.sh
```

## 看进度

```bash
tail -f /ssd_data/tsy_study_venv/OptiGenesis_Lancet/logs/detached_latest.log
pgrep -af "run_baseline_t1|main.py"
```

## 完成后

与 `outputs_baseline_t1_v2_resnet50/` 按相同格式汇总外部 ROC-AUC，更新对比表。
