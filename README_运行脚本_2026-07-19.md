# 根目录运行脚本说明

> **日期**：2026-07-19

仓库根目录**只保留**：

- `run_experiment.sh` — 绑定数据集 / `--detach` 后台跑任务

第三版（及更早）的 `run_baseline_*`、`run_ablation_*`、`run_comparison_*` 等已全部移至：

**`scripts/paper_v3_run/`**（见该目录 `README_2026-07-19.md`）

调用示例：

```bash
./run_experiment.sh --detach ./tsy_loho ./scripts/paper_v3_run/run_baseline_t0_oct_only.sh
```
