# 根目录运行脚本说明

> **日期**：2026-07-19（2026-07-20 补充 v4）

仓库根目录**只保留**：

- `run_experiment.sh` — 绑定数据集 / `--detach` 后台跑任务

第三版脚本：`scripts/paper_v3_run/`  
第四版脚本：`scripts/paper_v4_run/`（见该目录 README）

```bash
# v4 B1 T0（mean + 患者级 EDL，新 train/val/external 协议）
./run_experiment.sh --detach ./tsy_loho ./scripts/paper_v4_run/run_b1_mean_edl_t0_oct_only.sh

# v4 Ours T0（帧级 EDL + 不确定加权，无稳定模块）
./run_experiment.sh --detach ./tsy_loho ./scripts/paper_v4_run/run_ours_uw_agg_t0_oct_only.sh

# v3 归档复现
./run_experiment.sh --detach ./tsy_loho ./scripts/paper_v3_run/run_baseline_t0_oct_only.sh
```
